rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    function signedIn() {
      return request.auth != null;
    }

    function isOwner() {
      return signedIn() && request.auth.token.email != null;
    }

    match /workers/{workerId} {
      allow read: if signedIn();
      allow create, update, delete: if signedIn();
    }

    match /admins/{adminId} {
      allow read: if signedIn();
      allow create, update, delete: if signedIn();
    }

    match /tasks/{taskId} {
      allow read: if signedIn();
      allow create, update, delete: if signedIn();
    }

    match /messages/{messageId} {
      allow read: if signedIn();
      allow create: if signedIn()
        && request.resource.data.text is string
        && request.resource.data.text.size() > 0
        && request.resource.data.text.size() <= 500
        && request.resource.data.sender is string
        && request.resource.data.target is string;
      allow update: if false;
      allow delete: if signedIn();
    }

    match /config/{document} {
      allow read: if true;
    }

    match /production_raw_materials/{docId} {
      allow read, write: if signedIn();
    }
    match /production_colours/{docId} {
      allow read, write: if signedIn();
    }
    match /production_sizes/{docId} {
      allow read, write: if signedIn();
    }
    match /production_measurements/{docId} {
      allow read, write: if signedIn();
    }
    match /production_suppliers/{docId} {
      allow read, write: if signedIn();
    }
    match /dailyProductions/{docId} {
      allow read, write: if signedIn();
    }
    match /inventory_transactions/{docId} {
      allow read, write: if signedIn();
    }
  }
}
